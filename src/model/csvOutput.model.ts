export interface CsvOutput {
    uuid: string | null,
    vin: string | null,
    make: string | null,
    model: string | null,
    mileage: number | null,
    year: number | null,
    price: number | null,
    zipCode: number | null,
    createDate: Date | null,
    updateDate: Date | null,
}